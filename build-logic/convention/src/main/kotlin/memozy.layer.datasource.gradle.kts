/**
 * DataSource 계층 Convention Plugin
 *
 * 적용: KMP Library + Koin
 * 사용 대상: datasource/*/impl 모듈
 *
 * Note: Room KMP는 Phase 4에서 추가 예정
 */
plugins {
    id("memozy.library")
    id("memozy.koin")
}
