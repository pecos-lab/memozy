/**
 * Feature 계층 Convention Plugin
 *
 * 적용: KMP Library + Compose Feature + Koin
 * 사용 대상: feature/*/impl 모듈
 */
plugins {
    id("memozy.library")
    id("memozy.compose.feature")
    id("memozy.koin")
}
